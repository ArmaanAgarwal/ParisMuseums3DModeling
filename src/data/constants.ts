// Shared constants for consistent UI behavior across the museum

/**
 * Number of objects to show in preview cards (exhibits, zones, etc.)
 * This ensures consistency between preview and full views
 */
export const PREVIEW_OBJECT_COUNT = 4;

/**
 * Maximum number of featured objects to display in zone previews
 */
export const MAX_FEATURED_OBJECTS = 4;







